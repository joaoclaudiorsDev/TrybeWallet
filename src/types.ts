export type UserData = {
  name: string,
  email: string,
  cpf: string,
  address: string,
  city: string,
  uf: string,
};

export type WalletData = {
  resume: string,
  role: string,
  description:string,
};
